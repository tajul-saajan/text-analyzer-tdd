name: hello workflow

on: [push]

jobs:
    say-hello:
        runs-on: ubuntu-latest

        steps:
            - name: checkout code
              uses: actions/checkout@v3

            - name: say hello
              run: echo "👋 Hello from GitHub Actions!"

